# 복지 계산기 프로젝트 인수인계 문서 (v4.0)

안녕하세요! 이전 세션에서 개발한 복지 계산기 프로젝트를 이어서 진행하려고 합니다.
전체적인 흐름과 현재 상태를 설명드립니다.

---

## 📋 프로젝트 개요

**프로젝트명:** 8세 이하 아동 복지 계산기  
**목적:** 사용자의 조건에 맞는 복지 혜택을 자동으로 계산하고 추천  
**데이터 출처:** 복지로 API (XML)  
**데이터 범위:**
- 울산광역시 지자체 복지: 33개 서비스
- 중앙부처 복지: 101개 서비스
- 총 134개 서비스

**기술 스택:**
- 언어: Python (AI 자동매매 개발자, Python/AI/DB 초보)
- AI: OpenAI GPT-4o-mini (데이터 파싱)
- DB: MariaDB 10.5 (192.168.56.82, work_local)
- 파싱: XML → JSON → DB

---

## 🔄 전체 워크플로우

```
[1단계] 복지로 API
   ↓ (XML 다운로드)
[2단계] welfare_parser_v4.0.py
   ↓ (GPT 파싱 → JSON)
[3단계] welfare_db_converter_v4.0.py
   ↓ (JSON → MariaDB)
[4단계] DB 검색
   ↓ (사용자 조건 매칭)
[결과] 맞춤 복지 혜택 제공
```

---

## 🎯 핵심 설계 원칙 (v4.0)

### ⭐⭐⭐ 가장 중요한 결정 ⭐⭐⭐

**"모든 조건은 Benefits에 포함"**

이전 버전(v3.2)에서는 서비스 레벨에 공통 조건을 두고, benefits는 혜택 정보만 가졌습니다.
하지만 이 방식은 **혜택별로 조건이 다른 경우**(예: 0세 100만원, 1세 50만원)를 표현할 수 없었습니다.

**v4.0의 해결책:**
- 서비스 레벨: 지역 정보만 (sido, sigungu)
- Benefits 레벨: 모든 조건 포함 (나이, 소득, 출생순서, 양육방식 등)

**예시:**
```json
{
  "service_id": "WLF00004657",
  "service_name": "부모급여 지원",
  "sido": "울산광역시",  // ← 서비스 레벨 (지역만!)
  
  "benefits": [
    {
      "amount": 1000000,
      "description": "0세 가정 양육",
      "and_conditions": {  // ← 혜택별 조건!
        "age_min_months": 0,
        "age_max_months": 11,
        "childcare_type": "가정",
        "requires_citizenship": true
      }
    },
    {
      "amount": 500000,
      "description": "1세 가정 양육",
      "and_conditions": {  // ← 다른 조건!
        "age_min_months": 12,
        "age_max_months": 23,
        "childcare_type": "가정",
        "requires_citizenship": true
      }
    }
  ]
}
```

**이유:**
1. 혜택별로 조건이 다름 (0세 vs 1세)
2. 조건 중복 허용 (각 benefit마다 반복)
3. 검색 간단 (benefits 테이블만 조회)
4. 정확한 매칭 (0세 조건이면 0세 혜택만 반환)

---

## 🗄️ DB 스키마 (v4.0)

### 테이블 구조 (2개만!)

```sql
-- 1. welfare_services (서비스 기본 정보 + 지역)
CREATE TABLE welfare_services (
  service_id VARCHAR(50) PRIMARY KEY,
  service_name VARCHAR(200),
  sido VARCHAR(50),      -- ⭐ 지역은 여기만!
  sigungu VARCHAR(50),
  detail_url TEXT,
  source VARCHAR(100)
);

-- 2. welfare_benefits (혜택 + 모든 조건)
CREATE TABLE welfare_benefits (
  benefit_id INT PRIMARY KEY AUTO_INCREMENT,
  service_id VARCHAR(50),
  
  -- 혜택 정보
  amount DECIMAL(15,2),
  amount_type VARCHAR(50),
  benefit_type VARCHAR(50),
  payment_cycle VARCHAR(50),
  description TEXT,
  
  -- ⭐ AND 조건 (50개 이상의 필드!)
  age_min_months INT,
  age_max_months INT,
  birth_order INT,
  income_type VARCHAR(50),
  income_max_percent INT,
  childcare_type VARCHAR(50),
  household_members_min INT,
  household_members_max INT,
  requires_citizenship BOOLEAN,
  requires_disability BOOLEAN,
  child_has_serious_disease BOOLEAN,
  parent_has_serious_disease BOOLEAN,
  ... (총 50개 이상)
  
  -- ⭐ OR 조건 (JSON)
  or_conditions JSON,
  
  FOREIGN KEY (service_id) REFERENCES welfare_services(service_id)
);
```

**삭제된 테이블:**
- ❌ welfare_and_conditions (v3.2에서 사용)
- ❌ welfare_or_conditions (v3.2에서 사용)

**이유:** Benefits 중심 구조로 통합!

---

## 📁 주요 파일 설명

### 1. welfare_parser_v4.0_완전판.py

**역할:** 복지로 XML → 정형화 JSON

**핵심 기능:**
- OpenAI GPT-4o-mini로 비정형 텍스트 파싱
- Benefits 중심 구조로 변환
- Rate limit 재시도 로직
- 성공/실패 통계

**출력:**
```
정형화데이터_울산_v4.0_1211_1600.json
```

**GPT 프롬프트 핵심:**
```
❌ 절대 금지:
{
  "and_conditions": {...},  // 최상위 레벨 금지!
  "benefits": [...]
}

✅ 필수:
{
  "benefits": [
    {
      "amount": 1000000,
      "and_conditions": {...},  // 혜택별!
      "or_conditions": {...}
    }
  ]
}
```

---

### 2. DB_v4.0_완전판.sql

**역할:** MariaDB 스키마 생성

**핵심:**
- 2개 테이블만 (services, benefits)
- Benefits에 모든 조건 포함
- 50개 이상의 조건 필드
- 검색 최적화 인덱스

**실행:**
```bash
mysql -h 192.168.56.82 -u root -p work_local < DB_v4.0_완전판.sql
```

---

### 3. welfare_db_converter_v4.0_완전판.py

**역할:** JSON → MariaDB 삽입

**핵심 기능:**
- 자동 파일 탐지 (정형화데이터_*_v4.0_*.json)
- 서비스 삽입 (지역 포함)
- Benefits 삽입 (조건 포함)
- or_conditions JSON 처리

**실행:**
```bash
python3 welfare_db_converter_v4.0_완전판.py
# .env 파일에서 DB 설정 자동 로드
```

---

## 🔍 검색 로직

### 단일 쿼리로 완료!

```sql
SELECT 
  s.service_name,
  b.amount,
  b.description
FROM welfare_services s
INNER JOIN welfare_benefits b ON s.service_id = b.service_id
WHERE
  -- 지역 (서비스 레벨)
  (s.sido = '울산광역시' OR s.sido IS NULL)
  
  -- 나이 (혜택 레벨)
  AND (b.age_min_months IS NULL OR b.age_min_months <= :child_age)
  AND (b.age_max_months IS NULL OR b.age_max_months >= :child_age)
  
  -- 소득 (혜택 레벨)
  AND (b.income_type IS NULL OR b.income_max_percent >= :income)
  
  -- 출생순서 (혜택 레벨)
  AND (b.birth_order IS NULL OR b.birth_order = :birth_order)
  
  -- 양육방식 (혜택 레벨)
  AND (b.childcare_type IS NULL OR b.childcare_type = :childcare_type)

ORDER BY b.amount DESC;
```

**특징:**
- ✅ 1개 테이블 JOIN만
- ✅ 정확한 혜택만 반환
- ✅ 조건 누락 없음

---

## 🎯 주요 파싱 규칙

### 1. 나이 vs 출생순서 구분 (매우 중요!)

❌ **잘못된 예시:**
```
원문: "0세 100만원, 1세 50만원"
→ birth_order: 1, birth_order: 2  // 완전히 틀림!
```

✅ **올바른 예시:**
```
원문: "0세 100만원, 1세 50만원"
→ age_max_months: 11, age_max_months: 23  // 정답!
→ birth_order: null

원문: "첫째 70만원, 둘째 250만원"
→ birth_order: 1, birth_order: 2  // 정답!
```

**규칙:**
- "0세", "1세", "2세" = 나이 (age_*_months)
- "첫째", "둘째", "셋째" = 출생순서 (birth_order)

---

### 2. 조건이 다르면 별도 benefit

**예시 1:** 0세 100만원, 1세 50만원
```json
// ✅ 2개 benefit (나이 다름)
{
  "benefits": [
    {"amount": 1000000, "and_conditions": {"age_max_months": 11}},
    {"amount": 500000, "and_conditions": {"age_max_months": 23}}
  ]
}
```

**예시 2:** 가정양육 100만원, 어린이집 46만원
```json
// ✅ 2개 benefit (양육방식 다름)
{
  "benefits": [
    {"amount": 1000000, "and_conditions": {"childcare_type": "가정"}},
    {"amount": 460000, "and_conditions": {"childcare_type": "어린이집"}}
  ]
}
```

---

### 3. 공통 조건은 반복 OK

**예시:** "울산시민 중 0세는 100만원, 1세는 50만원"

```json
// ✅ 조건 중복 허용!
{
  "benefits": [
    {
      "amount": 1000000,
      "and_conditions": {
        "age_max_months": 11,
        "requires_citizenship": true  // 반복!
      }
    },
    {
      "amount": 500000,
      "and_conditions": {
        "age_max_months": 23,
        "requires_citizenship": true  // 반복!
      }
    }
  ]
}
```

---

### 4. payment_cycle 표준화

| amount_type | payment_cycle | 의미 | 계산 |
|-------------|---------------|------|------|
| 일시금 | "일시금" | 1회 지급 | amount × 1 |
| 분할 | "5회분할" | 5회 분할 | amount ÷ 5 |
| 월 | "매월" | 조건 끝날 때까지 | amount × months |
| 회 | null | 사용할 때마다 | amount × uses |

**규칙:**
- "일시금", "5회분할", "10회분할", "매월" (명확한 값!)
- amount_type: "월" + payment_cycle: null → "매월"로 간주

---

## 📊 버전 히스토리

### v3.0 → v3.1
- AND/OR 조건 분리
- 아동/부모 질환 구분

### v3.1 → v3.2
- household_members (가구원 수) 추가
- birth_order (출생순서) 추가
- benefits 표준화

### v3.2 → v4.0 ⭐⭐⭐
- **Benefits 중심 구조로 완전 전환**
- 서비스 레벨 조건 제거 (지역만 남김)
- welfare_and_conditions, welfare_or_conditions 테이블 삭제
- 검색 로직 단순화 (1개 JOIN만)

---

## 🚨 해결된 주요 이슈

### 이슈 1: 나이 조건 충돌
```
문제: and_conditions: {age_max: 12}, or_conditions: {age_max: 24}
→ 같은 필드가 AND/OR 양쪽에!

해결: Benefits별로 조건 분리
→ benefit[0]: {age_max: 12}, benefit[1]: {age_max: 24}
```

### 이슈 2: birth_order 오용
```
문제: "0세, 1세"를 birth_order: 1, 2로 파싱
→ 나이를 출생순서로 착각!

해결: GPT 프롬프트에 명시적 금지 규칙 추가
→ "0세, 1세"는 age_max_months: 11, 23
```

### 이슈 3: 지역 중복
```
문제: 모든 benefit에 sido, sigungu 반복
→ 중복 데이터!

해결: 지역은 서비스 레벨만
→ services.sido, services.sigungu
```

### 이슈 4: False 조건
```
문제: requires_grandparent_care: false
→ "조부모가 안 돌보는 경우"는 조건이 아님!

해결: GPT 프롬프트에 False 금지 규칙
→ true 또는 null만 허용
```

---

## 🔧 환경 설정

### DB 설정 (.env 파일)
```
OPENAI_API_KEY=sk-...
DB_HOST=192.168.56.82
DB_USER=root
DB_PASSWORD=qwer1234!
DB_NAME=work_local
```

### Python 패키지
```bash
pip install openai pymysql python-dotenv --break-system-packages
```

---

## 📝 실행 순서

### 전체 파이프라인
```bash
# 1. DB 스키마 생성
mysql -h 192.168.56.82 -u root -p work_local < DB_v4.0_완전판.sql

# 2. XML → JSON 파싱
python3 welfare_parser_v4.0_완전판.py
# → 정형화데이터_울산_v4.0_1211_1600.json

# 3. JSON → DB 삽입
python3 welfare_db_converter_v4.0_완전판.py
# → 자동으로 v4.0 파일 찾아서 처리

# 4. 검색 테스트
mysql -h 192.168.56.82 -u root -p work_local
> SELECT COUNT(*) FROM welfare_services;  -- 134개
> SELECT COUNT(*) FROM welfare_benefits;  -- ~400개 (서비스별 평균 3개)
```

---

## 🎯 다음 단계 (새 세션에서 진행할 내용)

### 1. 검색 모듈 개발
- Python 검색 함수 구현
- 사용자 입력 → SQL 생성
- 결과 포맷팅

### 2. 프론트엔드 연동
- API 엔드포인트 설계
- 검색 결과 반환
- 총 지원금 계산 로직

### 3. 테스트
- 다양한 조건 조합 테스트
- 정확도 검증
- 성능 최적화

---

## 💡 중요한 개념 정리

### 1. Benefits 중심 구조
- 모든 조건은 benefits에!
- 지역만 서비스 레벨!

### 2. 나이 vs 출생순서
- "0세, 1세" = age_*_months
- "첫째, 둘째" = birth_order

### 3. 조건 중복
- 각 benefit마다 반복 OK!
- 공통 조건도 각 benefit에!

### 4. 검색 단순화
- 1개 JOIN만!
- benefits 테이블이 핵심!

---

## 📚 참고 데이터 예시

### 부모급여 (WLF00004657)
```json
{
  "sido": "",  // 전국
  "benefits": [
    {"amount": 1000000, "age_max_months": 11, "childcare_type": "가정"},
    {"amount": 460000, "age_max_months": 11, "childcare_type": "어린이집"},
    {"amount": 500000, "age_max_months": 23, "childcare_type": "가정"},
    {"amount": 25000, "age_max_months": 23, "childcare_type": "어린이집"}
  ]
}
```

### 울주군 출산장려금
```json
{
  "sido": "울산광역시",
  "sigungu": "울주군",
  "benefits": [
    {"amount": 700000, "birth_order": 1, "payment_cycle": "일시금"},
    {"amount": 2500000, "birth_order": 2, "payment_cycle": "5회분할"},
    {"amount": 5000000, "birth_order": 3, "payment_cycle": "10회분할"}
  ]
}
```

---

## 🎉 핵심 요약

**v4.0의 가장 중요한 특징:**

1. **모든 조건은 Benefits에!**
   - 서비스 레벨: 지역만
   - Benefits 레벨: 나머지 모든 조건

2. **테이블 2개만!**
   - welfare_services (메타데이터)
   - welfare_benefits (혜택 + 조건)

3. **검색 간단!**
   - 1개 JOIN
   - 정확한 매칭

4. **확장 가능!**
   - 새 조건 추가? 컬럼 추가만!
   - 복잡한 마이그레이션 불필요!

---

이제 이 프로젝트를 이어받으실 준비가 되셨습니다! 
필요한 파일은 다음과 같습니다:
- welfare_parser_v4.0_완전판.py
- DB_v4.0_완전판.sql
- welfare_db_converter_v4.0_완전판.py

질문이나 추가 설명이 필요하시면 언제든 물어보세요! 🚀
